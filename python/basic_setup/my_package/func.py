def cap(s: str):
    """
        Upper-case the first letter
    """
    import pandas as pd # will fail without proper setup for remote or if pandas not installed in local
    pd.Dataframe()
    return s[0].upper() + s[1:]